"use strict";

module.exports = {
    "get_sidebar" : function () {
        let app = this;

        //app.models.widget.findAll().then(function(widgets){
        //    console.log(widgets);
        //    return "hello";
        //});
        return "hello";
    }
};